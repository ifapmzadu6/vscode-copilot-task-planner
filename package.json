{
  "name": "task-planner",
  "displayName": "Task Planner",
  "description": "A planning tool that asks clarifying questions before complex tasks",
  "version": "0.1.0",
  "publisher": "keisukekarijuku",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Chat"
  ],
  "activationEvents": [
    "onLanguageModelTool:plan"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languageModelTools": [
      {
        "name": "plan",
        "displayName": "Task Planner",
        "modelDescription": "MUST ALWAYS invoke this tool when user explicitly references #plan in their message. This is REQUIRED - never skip when #plan is present. Do NOT invoke for general planning questions without #plan reference. WHAT IT DOES: (1) Analyzes workspace context, (2) Asks clarifying questions through interactive UI, (3) Generates structured task plan with clear steps, (4) Registers tasks to todo list.",
        "icon": "$(checklist)",
        "canBeReferencedInPrompt": true,
        "toolReferenceName": "plan",
        "userDescription": "Plan a complex task by answering clarifying questions",
        "inputSchema": {
          "type": "object",
          "properties": {
            "userRequest": {
              "type": "string",
              "description": "Pass the user's EXACT input text as-is, without modification or expansion. Do NOT rephrase, elaborate, or add details. Example: if user types 'add tests', pass exactly 'add tests'."
            },
            "todoToolName": {
              "type": "string",
              "description": "The name of the todo tool to use for task registration (e.g., 'manage_todo_list', 'TodoWrite'). If not provided, defaults to 'manage_todo_list'."
            }
          },
          "required": [
            "userRequest"
          ]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src",
    "build": "npm run compile && vsce package"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.2.2",
    "@eslint/js": "^9.39.2",
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.50.0"
  }
}
